version: '3'
services:
        web:
                image: flask_8016_v1
                build: .
                ports:
                        - "8016:8036"
                volumes:
                        - ./volumes:/volumes
                        - ./instance:/instance
                restart: unless-stopped
